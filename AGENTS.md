# Codex Vault Rules

这是一个个人 Obsidian vault，用于日记、学习、研究项目、文献管理、实验记录、写作材料和可复用知识沉淀。

## 总体定位

- 本 vault 是“通用研究项目知识库”，不是数学建模专用系统。
- 支持数学建模、大创项目、课程论文、科研训练、毕业论文和其他研究项目。
- 数学建模可以有专项规则，但只能在数学建模项目或 `mathematical-modeling` skill 中生效，不能污染全局规则。

## 路径规则

- 所有具体项目放入 `Projects/`。
- 所有可复用领域知识放入 `Domains/`。
- 所有通用研究材料放入 `Research/`。
- 所有模板放入 `Templates/`。
- 所有索引和 MOC 放入 `MOCs/`。
- 人物、机构、课程、日记中抽取的实体放入 `Entities/`。
- 系统配置、工具清单、操作报告、AI 交接说明放入 `System/`。
- Codex skills 放入 `.agents/skills/`。
- 原始资料优先放入 `Research/Sources/`，也可以放入具体项目下的 `Sources/`。

## 安全规则

- 不要删除原始资料。
- 不要覆盖已有文件；如果目标文件已存在，必须先询问用户，或创建清晰命名的新版本。
- 不要修改日记正文，除非用户明确确认。
- 不要编造文献、DOI、网页来源、实验结果或引用内容；未核验来源的资料必须标注“待核验”，未读全文的论文不能当作已支持正文结论的证据。
- 不要编造作者、年份、期刊、会议、出版社、页码、BibTeX、数据来源、代码来源或实验参数。
- 论文、报告、综述和数学建模作品中的关键结论必须能追溯到数据、模型、实验、文献或用户提供的材料。
- 使用 AI 辅助写作时，最终引用真实性、事实准确性和学术诚信责任仍由用户确认。
- 高风险操作必须先列计划并等待确认。
- 高风险操作包括：批量移动、删除、重命名、批量改 frontmatter、大范围搜索替换、修改日记正文、处理原始资料。
- 工作区已有改动时，默认视为用户有意修改，不要回滚。

## 每次重要会话开始时先阅读

默认采用分层交接，避免占用过多上下文。

### 快速接手

日常接手时先读：

1. `AGENTS.md`
2. `System/Handoff/快速接手卡.md`
3. `System/Handoff/AI交接说明.md`
4. `System/Capabilities/系统能力地图.md`
5. 运行或查看 `git status --short`

### 按任务加载

如果任务涉及具体项目，再读该项目内的：

- `Overview.md`
- `Status.md`
- `Task List.md`
- `Decision Log.md`
- `Handoff Note.md`

如果任务涉及系统维护、规则修改、插件/skills、自动化或故障排查，再读：

- `README.md`
- `System/Tooling/工具安装清单.md`
- 最近 1-3 篇 `System/Reports/`
- 相关 `System/Workflows/`
- 相关 `.agents/skills/`

如果正在处理某个具体项目，还要阅读该项目内的状态、任务清单、决策记录和交接说明。

## 报告规则

- 每次重要写入、结构调整、批量整理后，都要在 `System/Reports/` 写一份简短报告。
- 报告应包含：日期、目标、改动文件、关键决定、风险或跳过事项、下一步建议。
- 纯只读探索一般不需要写报告，除非它影响后续工作。
- 报告不要只靠对话记忆；必须写入 vault 文件，方便未来 agent 交接。
- 长会话可以写一个当日持续报告，结束前补充“已完成 / 未完成 / 阻塞 / 下一步”。
- `System/Handoff/AI交接说明.md` 只保留当前状态摘要，不替代详细操作报告。

## Agent 交接规则

- 任何 agent 接手本 vault 时，必须先阅读“每次重要会话开始时先阅读”中列出的文件。
- 默认先使用 `System/Handoff/快速接手卡.md`，不要全量读取所有系统文件。
- 接手后先确认当前任务、已完成事项、未完成事项、阻塞原因和下一步。
- 长任务结束前必须更新 `System/Handoff/AI交接说明.md`。
- 如果安装插件、初始化 Git、迁移资料或调整结构，必须更新对应报告或清单。
- 如果某一步被网络、权限或用户确认阻塞，要明确写入“阻塞原因”和“恢复方式”。
- 不要依赖上一位 agent 的口头记忆；以 vault 内记录为准。
- 交接时优先阅读最近 3 篇 `System/Reports/`，再决定是否需要回看更早报告。

## Agent 分工规则

本 vault 可由 Codex 和 OpenCode 共同维护。Codex 不能默认直接控制 OpenCode；二者通过 vault 文件、任务清单、报告和 Git 状态协作。

### 低风险任务

以下任务优先交给 OpenCode + DeepSeek：

- 创建文件夹
- 创建模板初稿
- 扫描笔记
- 生成候选清单
- 生成 MOC 初稿
- 文献摘要初稿
- 项目状态更新
- 批量但可回滚的 Markdown 整理

### 高风险任务

以下任务优先交给 Codex：

- 修改 `AGENTS.md` 的核心规则
- 大规模移动文件
- 修改日记正文
- 给日记批量添加双链
- 设计研究项目架构
- 审查数学模型
- 审查实验方案
- 检查代码逻辑
- 写最终论文结构
- 做跨文件重构

### 共同规则

- 所有 agent 必须先阅读 `AGENTS.md`。
- 所有 agent 必须优先使用 `.agents/skills/` 中的 skills。
- 高风险操作必须先输出计划，等待用户确认。
- 不要删除原始资料。
- 不要覆盖已有文件。
- 每次重要修改后写入修改报告。
- 每次接手前运行或查看 Git 状态，避免覆盖其他 agent 的改动。
- 如果发现另一个 agent 的未提交改动，不要回滚，先记录并询问用户。

## 可复用内容固化规则

- 聊天中如果出现可复用的流程、判断标准、提示词、模板、检查清单、安全边界或项目经验，agent 应主动提醒用户是否固化。
- 固化形式按用途选择：
  - 可执行流程：写入 `System/Workflows/`
  - Codex 行为规则或专项能力：写入 `.agents/skills/`
  - 可复制笔记结构：写入 `Templates/`
  - 系统能力说明：更新 `System/Capabilities/系统能力地图.md`
  - 长期规则：更新 `AGENTS.md`
  - 项目经验：写入项目复盘，必要时提炼到 `Domains/` 或 `Research/Methods/`
- 固化前要说明会创建或修改哪些文件；涉及全局规则或 skill 行为变化时，必须先征求用户确认。
- 固化后要更新操作报告，并在必要时提交 Git。
- 不要把临时想法、未经验证的结论或一次性偏好写成长期规则。

## 笔记风格

- 使用适合 Obsidian 的 Markdown。
- 重要概念、项目、方法、数据集、文献和实体尽量使用双链。
- 需要被 Dataview 或插件检索的笔记可以添加简洁 frontmatter。
- 模板要实用，不要堆太多字段。
- 区分事实、解释、决定和行动项。

## 分类原则

- 项目笔记：`Projects/`
- 可复用知识：`Domains/`
- 通用研究材料：`Research/`
- 模板：`Templates/`
- 索引：`MOCs/`
- 日记实体：`Entities/`
- 系统配置和交接：`System/`
- Codex 工作流：`.agents/skills/`

## 项目边界

- 具体项目的假设、任务、文件和结论留在项目内。
- 可跨项目复用的方法、概念、写作结构、模型和经验，再提炼到 `Domains/` 或 `Research/`。
- 不要让某一种项目类型定义整个 vault 的工作方式。
