---
type: operation-report
date: 2026-05-05
project: 碳化硅外延层厚度确定研究
---

# 2026-05-05 碳化硅项目 Zotero 文献管理规则报告

## 目标

用户已安装 Zotero，并要求后续查到的文献原文、PDF、网页快照和附件优先保存到 Zotero；项目 vault 内只保留笔记、精读记录、方法节点和引用追踪。

## 本机检查

- 检测到 Zotero 可执行文件：`C:\Program Files\Zotero\zotero.exe`
- 检测到 Zotero 数据目录：`C:\Users\A2826\Zotero\`
- 检测到 Zotero 数据库：`C:\Users\A2826\Zotero\zotero.sqlite`

## 改动文件

- `Projects/MathematicalModeling/碳化硅外延层厚度确定研究/Notes/项目深挖工作规则.md`
- `Projects/MathematicalModeling/碳化硅外延层厚度确定研究/Literature/Zotero文献归档清单-2026-05-05.md`
- `Projects/MathematicalModeling/碳化硅外延层厚度确定研究/Handoff Note.md`
- `System/Handoff/AI交接说明.md`

## 关键规则

- 新查到的论文、标准、网页资料，优先建立 Zotero 条目，并在 Zotero 中保存 PDF、网页快照或链接。
- vault 中只写笔记：精读笔记、方法总结、引用追踪、模型来源说明、Zotero 待导入清单和已导入状态。
- 不直接编辑 Zotero 的 `zotero.sqlite` 数据库。
- 若 Codex 当前不能直接调用 Zotero API，应先生成待导入清单，列出 DOI、题名、用途、是否已有 PDF、证据等级和下一步导入方式。
- 已在项目 `Sources/Literature/` 的旧 PDF 和网页缓存视为历史资料，不自动删除或移动；后续迁移到 Zotero 后再由用户确认是否清理副本。

## 新增 Zotero 清单

新增 `Literature/Zotero文献归档清单-2026-05-05.md`，包含：

- 已精读核心文献：Li2010、Dong2012；
- 待精读核心文献：Oishi2006、Sun2023、Dutta2022、Chahal2024；
- 光学常数与数据库：Larruquert2011、Polyanskiy2024 等；
- TMM 与薄膜反演方法：Troparevsky2010、MacMillan1992、Byrnes tmm；
- 近年算法与机器学习线索；
- 竞赛官方与优秀论文资料。

## 风险与跳过事项

- 本轮未直接导入 Zotero，避免在未确认 API/Better BibTeX/导入流程前误改文献库。
- 本轮未删除项目中的旧 PDF 或网页缓存。
- 未全文精读的条目仍保持“待精读”证据等级，不能因为进入 Zotero 就作为论文强证据。

## 下一步建议

- 在 Zotero 中建立集合 `SiC 外延层厚度确定研究` 及子集合。
- 优先导入 Li2010 和 Dong2012，并将 Zotero citekey 或 select URI 回填到对应精读笔记。
- 若用户希望自动化导入，可选择 RIS/BibTeX 导入、Better BibTeX 或 Zotero Web API 三种安全路径之一。

## 补充执行记录：已导入 Zotero

用户随后要求直接在 Zotero 里建立集合并导入条目。本轮已完成：

- 通过 Zotero 本地 connector `/connector/import` 导入 26 条项目相关文献/网页条目。
- 创建集合并最终命名为 `SiC Epilayer Thickness Study`。
- Zotero collection key：`89BU9R2E`。
- API 验证集合内条目数为 26。

注意事项：

- Zotero connector 对中文集合名和中文标签显示不稳定，曾显示为问号，因此集合名改为英文。
- 本次只导入元数据、DOI/URL 和说明，没有批量绑定 PDF 附件。
- 为修正集合名，曾在关闭 Zotero 后进行最小数据库字段修改，并在操作前备份：

```text
C:\Users\A2826\Zotero\zotero.sqlite.backup-before-sic-rename-20260505
```

后续仍应优先使用 Zotero connector/API/标准导入文件，不把直接数据库修改作为常规流程。

## 补充纠错记录

Zotero 本地 API 对中文集合名会显示为问号。操作过程中曾误把另一个项目集合 `新疆电量预测与产业因果推断研究` 识别为乱码残留集合并尝试清理。随后已从备份恢复该集合及其 collectionItems 关系。

当前 Zotero 集合状态：

| collection key | 集合名 | 条目数 | 说明 |
| --- | --- | ---: | --- |
| `89BU9R2E` | `SiC Epilayer Thickness Study` | 26 | 本 SiC 项目集合 |
| `RIKW96JZ` | `新疆电量预测与产业因果推断研究` | 30 | 另一个项目集合，后续不要改动 |

相关备份：

```text
C:\Users\A2826\Zotero\zotero.sqlite.backup-before-sic-rename-20260505
C:\Users\A2826\Zotero\zotero.sqlite.backup-before-sic-cleanup-20260505
```

## 补充核查记录：界面已定位到项目集合

用户反馈在 Zotero 中暂时看不到条目后，已再次核查本地 Zotero API：

| collection key | 集合名 | 条目数 |
| --- | --- | ---: |
| `89BU9R2E` | `SiC Epilayer Thickness Study` | 26 |
| `RIKW96JZ` | `新疆电量预测与产业因果推断研究` | 30 |

已通过 `zotero://select/library/collections/89BU9R2E` 将 Zotero 界面定位到本项目集合。项目内 `Literature/Zotero文献归档清单-2026-05-05.md` 已同步改为 `imported-to-zotero` 状态，并补充“如何查看集合”的说明。

## 补充修复记录：清理问号和作者不完整

用户反馈 Zotero 集合里存在大量问号、作者字段不清楚。已核查原因：第一轮导入使用简化 RDF 元数据，部分作者仅写成 `et al.`，中文网页标题/标签经过 Zotero connector 后显示为问号。

修复步骤：

1. 通过 Zotero 本地 API 导出项目集合 26 条 JSON 备份：

```text
Projects/MathematicalModeling/碳化硅外延层厚度确定研究/Literature/ZoteroBackups/zotero-sic-collection-before-metadata-cleanup-2026-05-05.json
```

2. 使用 DOI 官方 CSL JSON 元数据重建清洁 RDF：

```text
Projects/MathematicalModeling/碳化硅外延层厚度确定研究/Literature/Zotero-import-SiC-clean-metadata-2026-05-05.rdf
```

3. 通过 Zotero connector 重新导入清洁条目。Zotero 本地 `/api/users/0` 写接口对 `PATCH` 返回 `501 Method not implemented`，所以不能直接 API 修旧条目。
4. 为避免用户继续看到坏条目，已关闭 Zotero，备份数据库，并最小调整 collection membership：`SiC Epilayer Thickness Study` 只保留清洁导入的 26 条，旧坏条目未删除，只是不再挂入项目集合。

数据库备份：

```text
C:\Users\A2826\Zotero\zotero.sqlite.backup-before-sic-metadata-cleanup-20260505
```

修复后核查：

```text
RIKW96JZ | 新疆电量预测与产业因果推断研究 | 30
89BU9R2E | SiC Epilayer Thickness Study | 26
bad_question_items=0
empty_creator_items=0
```

`tmm: transfer matrix method Python package` 作为 `computerProgram` 类型导入时 creator 字段曾为空。已补 programmer 为 Steven J. Byrnes。补充前备份：

```text
C:\Users\A2826\Zotero\zotero.sqlite.backup-before-sic-tmm-creator-fix-20260505
```
